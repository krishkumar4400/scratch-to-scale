function greet(person: string): string {
    return `Hello ${person}`;
}

const username:string = "krish kumar";

console.log(greet(username));

let name = "krish"
// name = 5;

let cups = Math.random() > 0.5 ? 10 : '5' // inferencing

let age:number = 21; // annotation (we are annotating)
age = 22;

// age = "30" 


